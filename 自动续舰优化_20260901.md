# 自动续舰优化 + 舒克游戏返回按钮

## 需求（2026-09-01）
1. 自动续舰切换按钮显示不清，分不清点了没点
2. 导出时自动续舰的人也要合并进当月导出表格
3. 勾选自动续舰时弹提示框（可爱 icon + 说明文案）
4. 舒克养成游戏加返回主页按钮
5. 完成后自检、删冗余代码

## 改动

### captain.js
- `exportData()`：导出当月数据时，先拉取续舰名单（renew_captains），解密后**按昵称去重合并**进当月导出（自动续舰的人一起导出，方便使用）
- `onAutoRenewToggle()` 拆分为：
  - `refreshAutoRenewUI()`：只刷新开关显示状态（不弹框），供 openForm 编辑回填时调用
  - `onAutoRenewToggle()`：刷新 + 勾选时弹提示框
- 新增 `openAutoRenewHint()` / `closeAutoRenewHint()`
- 删除死代码：`copyToNextMonth()`、`getNextMonth()`（之前自动复制到下月 records 的错误实现，已废弃）

### index.html
- 新增「自动续舰提示弹框」`#arh`（复用 .cd/.cx 结构），含 🐱 icon + 说明文字「勾选后接下来都不再需要填写地址，如有疑问可咨询小光」
- 续舰名单管理弹框已有「导出」按钮（上轮加的）

### app.css
- 新增 `.arh-icon/.arh-title/.arh-desc` 提示框样式 + 弹跳动画
- 自动续舰开关样式增强：选中态整行高亮（.renew-toggle.on）

### shuke.html / shuke.css
- 顶部栏左侧新增返回主页按钮 `‹`（onclick location.href='./'）

## 自检结果
- JS 语法：captain/shuke/games/gift-showcase/cotton-candy/playlist 全部 exit 0
- CSS 大括号：app.css 196/196、shuke.css 139/139、games.css 339/339、gift-showcase.css 187/187、cotton-candy.css 149/149、playlist.css 88/88 全部 OK
- 无重复函数定义；无 getNextMonth/copyToNextMonth 残留

## 提交
- commit `fac546c`，已推送 master（origin/master = HEAD = fac546c）

---

## 二改（2026-09-01 10:13）
- **舰长类型**：toggle 滑块 → 分段选择器（📝 手动填写 / 🔄 自动续舰 两个大按钮，选中高亮）
- **续舰名单**：删「导出」按钮，新增「编辑」功能（增删改查），加 dbUpdateRenewCaptain + editRenewCaptain + editingRenewId 状态
- **导出整合**：删 header「导出」按钮，导出入口统一进「历史」弹窗（顶部「📤 导出当月舰长+续费舰长」按钮），历史按钮文案改「📅 历史/导出」
- 删除 exportRenewList / refreshAutoRenewUI / onAutoRenewToggle 旧函数
- commit `5da196d` 已推送 master

### 关键：git 推送认证
- remote URL 曾写死失效 PAT `ghp_...`，导致 push 报「Invalid username or token」
- 有效凭据在 git credential manager（OAuth token `gho_`，user=G-Luminos，有 push 权限）
- 直连 GitHub 会「Connection reset」，需走 ghfast.top 代理
- **正确推法**：`git remote set-url origin https://G-Luminos:<gho_token>@ghfast.top/github.com/G-Luminos/fuguang.git` 然后 `git -c credential.helper= push`；推完恢复 remote URL 为 `https://github.com/G-Luminos/fuguang.git`（避免 token/代理残留）
