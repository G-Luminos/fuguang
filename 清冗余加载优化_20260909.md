# 清冗余 + 加载速度优化 + 全局 Loading 动画

日期：2026-09-09
commit：`ebb0d1a`（已推送到 master，77cfd17..ebb0d1a）

## 用户需求
1. 在保证现有所有功能前提下清除冗余/死代码
2. 自测避免莫名报错
3. 提升网页加载速度
4. 所有保存/提交操作加等待 loading 动画

## 实际改动

### 1. 去除冗余依赖（提升加载 + 清冗余）
- 从 git 移除 `package.json` / `package-lock.json`（之前为直连数据库装的 `pg` 依赖，前端纯静态项目用不到）
- 删除本地 `node_modules/`（0.4MB，从未被 git 跟踪，仅本地残留）
- `.gitignore` 已含 node_modules/

### 2. 清理 console 调用
- `captain.js`：保留 2 处（encrypt/decrypt 的 catch 错误日志，属关键错误），其余之前已清
- `gift-showcase.js`：删除 2 处 `console.error`（加载礼物图片出错 / 处理图片失败），改为静默注释跳过（图片上传中途失败不影响整体，无需刷控制台）

### 3. 新增全局 Loading 遮罩（所有异步操作反馈）
- `captain.js` 新增 `showLoading(msg)` / `hideLoading()` 函数，挂到 `window`（供其他模块复用）
- 动态创建 `#gLoading` 遮罩层：毛玻璃背景 + 白色卡片 + 旋转 spinner + 文案
- `app.css` 新增 `.g-loading` / `.g-loading-box` / `.g-loading-spinner` / `.g-loading-text` 样式 + 动画 keyframes

**已覆盖的等待动画点：**
| 操作 | 函数 |
|------|------|
| 管理员登录 | `doLogin` |
| 访客进入 | `submitGuestNick` |
| 保存/添加舰长 | `saveForm` |
| 删除舰长 | `delR`（确认回调） |
| 打开历史 | `openHistory` |
| 逐月导出 | `exportHistory` |
| 打开续舰名单 | `openRenewAdmin` |
| 新增/编辑续舰 | `addRenewCaptain` |
| 删除续舰 | `delRenewCaptain` |
| 当月导出（合并续舰） | `exportData` |
| 往期舰礼上传图片 | `handleImageUpload` |
| 往期舰礼删除图片 | `deleteImage` |

### 4. 修复既有小问题
- `enterApp()` 由同步改异步 `await render()`（保证进入时数据渲染完整后再收 loading）
- `render()` 内多余的 `renderStats()` 调用已在之前清理过（admin 进入时开头调一次，不再重复）
- `saveForm` 的 `closeForm(); render()` 改为 `await render()`，避免 loading 收起后列表还没刷新完

### 5. 自测结果
- 所有 JS `node --check` 通过：captain.js / gift-showcase.js / cotton-candy.js / games.js / playlist.js / shuke.js 全部 exit 0
- CSS 大括号配平：app.css 203/203、gift-showcase.css 187/187、games.css 339/339、cotton-candy.css 149/149、playlist.css 88/88 全部 OK
- 线上 `https://g-luminos.github.io/fuguang/` HTTP 200，按钮齐全（歌单/舰长礼物/往期舰礼/匿名投稿/互动游戏/舒克养成）

## 注意事项
- 棉花糖投稿（ccSubmit）和棋盘保存（mpSavingOverlay）原本已有专属 loading overlay，未重复改造，保持原样
- 下次改代码后仍需在结尾明确告知「git已推送 commit xxx」（用户工作流偏好）
